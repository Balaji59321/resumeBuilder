// import React, { useContext, useReducer } from "react";

// const DataLayer = React.createContext();

// function DataProvider({ initialValue, reducer, children }) {
//   return (
//     <DataLayer.Provider value={useReducer(reducer, initialValue)}>
//       {children}
//     </DataLayer.Provider>
//   );
// }
// export default DataProvider;

// export const DataConsumer = () => useContext(DataLayer);
